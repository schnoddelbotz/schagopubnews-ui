import { helper } from '@ember/component/helper';

export default helper(function excerpt(params/*, hash*/) {
	// FIXME todo: add param maxLength. Cut there. Or be more text-sensual. Take from start and from end. Or so.
  return params;
});
